---
const { class: className, id } = Astro.props;
---

<section 
  id={id} 
  class={`w-full max-w-full sm:max-w-[95%] md:max-w-[90%] lg:max-w-[80%] mx-auto 
          px-6 sm:px-8 md:px-12 lg:px-16 ${className} pb-24`}>
  <slot/>
</section>
